import { Box, Typography } from "@mui/material";

const Product = () => {
  return (
    <Box sx={{ w: "100vw", h: "100vh", display: "flex" }}>
      <Typography sx={{ color: "white" }}> This is product page</Typography>{" "}
    </Box>
  );
};

export default Product;
